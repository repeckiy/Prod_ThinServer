# Thin-Server Configuration File
# 🎯 ЄДИНЕ ДЖЕРЕЛО для всіх налаштувань деплою
# Редагуй ТІЛЬКИ цей файл - зміни автоматично застосуються до всіх компонентів
#
# ВАЖЛИВО: Змінні БЕЗ 'export' для сумісності з systemd EnvironmentFile
# Bash скрипти мають використовувати 'set -a' перед sourcing цього файлу

# ============================================
# МЕРЕЖЕВІ НАЛАШТУВАННЯ
# ============================================
SERVER_IP="172.18.39.57"
RDS_SERVER="rds.dn-kronas.local"
NTP_SERVER="172.18.39.2"

# ============================================
# ШЛЯХИ (не змінюй без необхідності!)
# ============================================
APP_DIR="/opt/thinclient-manager"
WEB_ROOT="/var/www/thinclient"
TFTP_ROOT="/srv/tftp"
LOG_DIR="/var/log/thinclient"
DB_DIR="/opt/thinclient-manager/db"

# ============================================
# ВЕРСІЇ КОМПОНЕНТІВ
# ============================================
FREERDP_VERSION="3.17.2"
DEBIAN_VERSION="bookworm"

# ============================================
# FEATURES (увімкнути/вимкнути)
# ============================================
ENABLE_PRINT_SERVER=true
ENABLE_USB_REDIRECT=true
ENABLE_AUDIO=true
# SSH для діагностики тонких клієнтів
ENABLE_SSH=true
# Створювати GPU-специфічні initramfs варіанти
BUILD_VARIANTS="yes"

# ============================================
# СТИСНЕННЯ INITRAMFS (встановлюється при деплої)
# ============================================
# Буде встановлено автоматично при запуску install.sh
# Можливі значення: pigz, zstd, zstd-fast, lz4
# Алгоритм стиснення (за замовчуванням: zstd)
COMPRESSION_ALGO="zstd"
# Команда стиснення
COMPRESSION_CMD="zstd -19 -T0 -c"
# Команда розпакування
DECOMPRESSION_CMD="zstd -dc"

# ============================================
# ОБСЛУГОВУВАННЯ
# ============================================
LOG_RETENTION_DAYS=3
AUTO_CLEANUP=true
BACKUP_ENABLED=true
BACKUP_DIR="/opt/thin-server/backups"

# ============================================
# БЕЗПЕКА
# ============================================
DEFAULT_ADMIN_USER="admin"
DEFAULT_ADMIN_PASS="admin123"
REQUIRE_PASSWORD_CHANGE=true
SESSION_TIMEOUT_MINUTES=60

# ============================================
# SSH ДЛЯ ТОНКИХ КЛІЄНТІВ (діагностика)
# ============================================
# Пароль root на тонких клієнтах
THINCLIENT_SSH_PASSWORD="thinclient2025"
# SSH порт
THINCLIENT_SSH_PORT="22"